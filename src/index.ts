// const name = "chesedi",
// age = 24,
// gender = "male";

const sayHi = (name: string, age: number, gender: string): string => {
  return `hello ${name}, you ar ${age}, you are a ${gender}!!!@@@`;
}

console.log(sayHi("chesedi", 24, "male"));

export {};